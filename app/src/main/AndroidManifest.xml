<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ladwa.aditya.twitone">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".TwitoneApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!-- Activities -->
        <activity
            android:name=".login.LoginActivity"
            android:configChanges="orientation|screenSize"
            android:label="@string/app_name"
            android:launchMode="singleTop"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <activity
            android:name=".mainscreen.MainScreen"
            android:label="@string/app_name"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Recievers -->
        <receiver
            android:name=".util.ConnectionReceiver"
            android:enabled="true">
            <intent-filter>
                <action android:name="android.net.conn.CONNECTIVITY_CHANGE" />
            </intent-filter>
        </receiver>

        <!-- Content Provider -->
        <provider
            android:name=".data.local.TwitoneProvider"
            android:authorities="com.ladwa.aditya.twitone" />

        <!-- Meta Data -->
        <meta-data
            android:name="com.ladwa.aditya.twitone.util.GlideConfiguration"
            android:value="GlideModule" />

        <activity
            android:name=".interactions.Interactions"
            android:label="@string/title_activity_interactions"
            android:parentActivityName=".mainscreen.MainScreen"
            android:theme="@style/AppTheme">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.ladwa.aditya.twitone.mainscreen.MainScreen" />
        </activity>
    </application>

</manifest>